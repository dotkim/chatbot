services:
  app:
    build: 
      context: .
      dockerfile: Dockerfile
    volumes:
      - ../.:/chatbot:cached
    command: sleep infinity
    environment:
      - "ASPNETCORE_ENVIRONMENT=Development"
